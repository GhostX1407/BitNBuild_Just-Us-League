# ── Core framework ──────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3

# ── Database ─────────────────────────────────────────────────────────────────
# asyncpg 0.30.0+ ships pre-built wheels for Python 3.13/3.14
sqlalchemy==2.0.41          # AsyncEngine / AsyncSession (2.x API)
psycopg[binary]==3.3.6      # Async PostgreSQL driver (production) — Python 3.14 compatible
aiosqlite==0.21.0           # Async SQLite driver (test isolation, no PG needed)
greenlet>=3.1.1             # Required by SQLAlchemy AsyncEngine
alembic==1.16.1             # Schema migrations

# ── Validation / Settings ────────────────────────────────────────────────────
# pydantic 2.13.5+ ships pydantic-core with Python 3.14 pre-built wheels
pydantic==2.13.5
pydantic-settings==2.15.0    # BaseSettings with .env support
python-dotenv==1.1.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.26.0      # async test support
httpx==0.28.1               # async HTTPX client for TestClient
